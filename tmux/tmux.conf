set -g mouse on
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*:RGB"
set -g set-clipboard on
setw -g mode-keys vi
set -g history-limit 100000
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Pane separators — heavy UTF-8 box-drawing lines. The active pane is marked
# with an amber fisheye in the top-left corner (pane-border-format) rather
# than a different border color, echoing the active window pill. The border
# only appears with ≥2 panes: a lone pane would burn a row on a redundant
# marker, so a window-layout-changed hook flips pane-border-status per window
# off (default) / top based on window_panes. Seams join cleanly as long as
# WezTerm's line_height stays 1.0 — a non-1.0 line height splits box-drawing
# at every row boundary (wezterm issue #584), breaking corners.
set -g pane-border-lines heavy
set -g pane-border-style 'fg=#3b4261'
set -g pane-active-border-style 'fg=#3b4261'
set -g pane-border-status off
set -g pane-border-format '#[align=left]#{?pane_active,#[fg=#e0af68]◉#[default],}'
set-hook -g window-layout-changed 'if -F "#{>:#{window_panes},1}" "setw pane-border-status top" "setw pane-border-status off"'

# New sessions get random names (tmux defaults to 0, 1, 2…)
set-hook -g session-created 'run-shell "$HOME/.config/tmux/scripts/name-if-default.sh session #{session_name}"'

# Auto-name windows after the active pane's folder (lowercased).
# automatic-rename is off; a hook renames windows when you focus them,
# lowercasing the folder basename. (tmux has no native lowercase, and
# #{...} inside #(...) is unreliable in this build, so the hook + script
# queries the path itself.)
#
# pane-focus-in covers every case: it fires when a pane gains focus, which
# happens on both pane switches and window switches (selecting a window
# focuses its active pane). session-window-changed only covered the latter,
# leaving the status-bar window name stale after moving between panes.
setw -g automatic-rename off
setw -g allow-rename on
setw -g automatic-rename-format '#{?pane_dead,[dead],#{b:pane_current_path}}'
set-hook -g pane-focus-in 'run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"'

# Pane navigation. After each switch, rename the window to the now-active
# pane's folder (lowercase) — tmux fires no hook on intra-window pane changes,
# so we drive it from the nav keys. (cd is handled by zsh chpwd; window
# switches by the session-window-changed hook.)
bind -r h select-pane -L \; run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"
bind -r j select-pane -D \; run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"
bind -r k select-pane -U \; run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"
bind -r l select-pane -R \; run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"
bind -r o select-pane -t :.+ \; run-shell "$HOME/.config/tmux/scripts/lowername.sh #{pane_id}"

unbind r
bind -r r source-file "$HOME/.config/tmux/tmux.conf"

bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
unbind -T copy-mode-vi MouseDragEnd1Pane

# C-b w: S:/W:/P: labels; shortcuts start at 1
unbind w
bind w choose-tree -Z -F '#{?session_format,S: #{session_name},window_format,W: #{b:pane_current_path},#{?pane_marked,#[reverse],}P: #{b:pane_current_path}#{?pane_active,*,}#{?pane_marked,M,}}' -K '#{e|+|:line,1}'

# Status bar — session pill left, plain window list right
set-hook -gu client-attached
set-hook -gu client-session-changed
set -g status on
set -g status-style 'bg=default,fg=default'
set -g status-left ''
set -g status-right ''
set -g status-justify right
setw -g window-status-separator ''
setw -g window-status-format '#[fg=#a9b1d6,bg=default]#[fg=#1f2335,bg=#a9b1d6] #{window_name} #[fg=#a9b1d6,bg=default]#[default] '
setw -g window-status-current-format '#[fg=#9ece6a,bg=default]#[fg=#1f2335,bg=#9ece6a,bold] #{window_name} #[fg=#9ece6a,bg=default]#[default] '
# Two-line status: a transparent blank row sits between the panes and the
# status content, giving a small visual gap. With status at the top, content
# is status-format[0] (top edge); the gap is status-format[1] (just above panes).
set -g status-position top
set -g status 2
set -g status-format[0] '#[align=left]#[fg=#7C3AED,bg=default]#[fg=#FFFBF5,bg=#7C3AED,bold]◉ #{session_name} #[fg=#7C3AED,bg=default]#[default,nobold] #[align=right]#{W:#{E:window-status-format},#{E:window-status-current-format}}'
set -g status-format[1] '#[bg=default]'
